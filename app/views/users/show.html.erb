<h1><%= @user.name %></h1>

<h3> Comments </h3>
<% @user.comments.each do |comment| %>
	<div class="panel panel-default">
	  <div class="panel-body">
	  	<p><%= comment.user.id %> : </p>
	  	<p><%= comment.user.name %> said : </p>
	  	<p><%= comment.content %></p>
	  	 <%= form_tag [@user, comment], method: "delete" do %>
	      <button class="btn icon-preview mdi-action-delete">
	        <%= t(".delete") %>
	      </button>
	    <% end %>
	  </div>
	</div>
<% end %>

<div class="panel panel-default">
  <div class="panel-body">
    <%= form_for [@user, @comment] do %>
      <textarea row="5" name="content"></textarea>
      <input type="submit" value="Submit" />
    <% end %>
  </div>
</div>

<!-- link_to "/users/#{@user.id}?aaa=bbb&c=3"
link_to user_path(@user, aaa: "bbb", c: 3)
link_to @user -->

<%= link_to "Back to index", root_path %>
